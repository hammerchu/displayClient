import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.boardclick.app',
  appName: 'displayClient',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
